---
version: 2
publish: &publish
  working_directory: ~/repo
  docker:
    - image: circleci/node:10.4.1
  steps:
    - checkout
    - restore_cache:
        name: Restore npm package cache
        key: v1-cache-dependencies-{{ checksum "package-lock.lock" }}-{{ checksum  "package.json" }}-{{ arch }}
    - run:
        name: Installing dependencies
        command: npm ci
    - save_cache:
        name: Save npm package cache
        paths:
          - ~/.cache/npm
        key: v1-cache-dependencies-{{ checksum ""package-lock.lock" }}-{{ checksum  "package.json" }}-{{ arch }}
    - run:
        name: Run tests
        command: npm run tests
    - run:
        name: Login into Expo
        command: npx expo-cli login -u $EXPO_USERNAME -p $EXPO_PASSWORD
    - run:
        name: Publish to Expo
        command: npx expo-cli publish --non-interactive --max-workers 1 --release-channel $EXPO_RELEASE_CHANNEL

jobs:
  publish_to_expo_dev:
    environment:
      EXPO_RELEASE_CHANNEL: dev
    <<: *publish

  publish_to_expo_prod:
    environment:
      EXPO_RELEASE_CHANNEL: default
    <<: *publish

workflows:
  version: 2
  my_app:
    jobs:
      - publish_to_expo_dev:
          filters:
            branches:
              only: development
      - publish_to_expo_prod:
          filters:
            branches:
              only: master
